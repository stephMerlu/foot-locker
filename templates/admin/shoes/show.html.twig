{% extends 'base.html.twig' %}

{% block title %}Shoes{% endblock %}

{% block body %}
    <h1>Shoes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shoe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shoe.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ shoe.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ shoe.price }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ shoe.sexe }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ shoe.category }}</td>
            </tr>
            <tr>
                <th>ShoeSize</th>
                <td>{{ shoe.shoeSize }}</td>
            </tr>
            <tr>
                <th>QuatityStock</th>
                <td>{{ shoe.quatityStock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_shoes_index') }}">back to list</a>

    <a href="{{ path('app_admin_shoes_edit', {'id': shoe.id}) }}">edit</a>

    {{ include('admin/shoes/_delete_form.html.twig') }}
{% endblock %}
